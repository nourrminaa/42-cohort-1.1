[tool.poetry]
name = "loading"
version = "0.1.0"
authors = ["nmina"]

[tool.poetry.dependencies]
python = ">=3.11,<4.0"
numpy = "*"
pandas = "*"
matplotlib = "*"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"